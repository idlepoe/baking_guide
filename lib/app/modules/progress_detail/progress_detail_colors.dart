import 'package:flutter/material.dart';

/// Progress detail 화면 공통 색상 (목업 기준).
abstract final class ProgressDetailColors {
  static const accent = Color(0xFFFFC107);
  static const accentForeground = Color(0xFF1A1A1A);
  static const completedStep = Color(0xFFE0E0E0);
  static const inactiveStep = Color(0xFFBDBDBD);
}
